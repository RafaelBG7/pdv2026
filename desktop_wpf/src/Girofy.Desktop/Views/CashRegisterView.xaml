<UserControl x:Class="Girofy.Desktop.Views.CashRegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <ControlTemplate x:Key="PaymentMethodIconTemplate" TargetType="ContentControl">
            <Border Width="30" Height="30"
                    Background="{DynamicResource InfoSurfaceBrush}"
                    BorderBrush="{DynamicResource InfoBorderBrush}"
                    BorderThickness="{DynamicResource AccessibilityBorderThickness}"
                    CornerRadius="9">
                <Grid Width="18" Height="18" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Path Data="M2.2,4 L15.8,4 C17,4 18,5 18,6.2 L18,13.8 C18,15 17,16 15.8,16 L2.2,16 C1,16 0,15 0,13.8 L0,6.2 C0,5 1,4 2.2,4 Z M2,8 L2,6 L5,6 M16,12 L16,14 L13,14 M9,7.2 A2.8,2.8 0 1 0 9,12.8 A2.8,2.8 0 1 0 9,7.2"
                          Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="1.7"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers><DataTrigger Binding="{Binding Method}" Value="money"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                    <Path Data="M9,9 L5.15,5.15 C4.35,4.35 4.35,3.15 5.15,2.35 L6.65,0.85 C7.95,-0.45 10.05,-0.45 11.35,0.85 L12.85,2.35 C13.65,3.15 13.65,4.35 12.85,5.15 Z M9,9 L12.85,5.15 C13.65,4.35 14.85,4.35 15.65,5.15 L17.15,6.65 C18.45,7.95 18.45,10.05 17.15,11.35 L15.65,12.85 C14.85,13.65 13.65,13.65 12.85,12.85 Z M9,9 L12.85,12.85 C13.65,13.65 13.65,14.85 12.85,15.65 L11.35,17.15 C10.05,18.45 7.95,18.45 6.65,17.15 L5.15,15.65 C4.35,14.85 4.35,13.65 5.15,12.85 Z M9,9 L5.15,12.85 C4.35,13.65 3.15,13.65 2.35,12.85 L0.85,11.35 C-0.45,10.05 -0.45,7.95 0.85,6.65 L2.35,5.15 C3.15,4.35 4.35,4.35 5.15,5.15 Z"
                          Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="1.7"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers><DataTrigger Binding="{Binding Method}" Value="pix"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                    <Path Data="M2.2,2 L14.8,2 C16,2 17,3 17,4.2 L17,12.8 C17,14 16,15 14.8,15 L2.2,15 C1,15 0,14 0,12.8 L0,4.2 C0,3 1,2 2.2,2 Z M0,5.2 L17,5.2 M3,7.8 L6.8,7.8 L6.8,11 L3,11 Z M4.25,7.8 L4.25,11 M5.55,7.8 L5.55,11 M3,12.8 L9,12.8 M3,14 L7,14 M14.2,9.2 A3.8,3.8 0 1 0 14.2,16.8 A3.8,3.8 0 1 0 14.2,9.2 M12.2,13 L16.2,13"
                          Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="1.55"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers><DataTrigger Binding="{Binding Method}" Value="debit"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                    <Path Data="M2.2,2 L14.8,2 C16,2 17,3 17,4.2 L17,12.8 C17,14 16,15 14.8,15 L2.2,15 C1,15 0,14 0,12.8 L0,4.2 C0,3 1,2 2.2,2 Z M0,5.2 L17,5.2 M0,7.3 L17,7.3 M3,9.2 L6.7,9.2 L6.7,12 L3,12 Z M8,10 L12.2,10 M8,11.8 L10.5,11.8 M14.2,9.2 A3.8,3.8 0 1 0 14.2,16.8 A3.8,3.8 0 1 0 14.2,9.2 M12.1,13 L16.3,13 M14.2,10.9 L14.2,15.1"
                          Stroke="{DynamicResource PrimaryBrush}" StrokeThickness="1.55"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" StrokeLineJoin="Round" Stretch="Uniform">
                        <Path.Style>
                            <Style TargetType="Path">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Style.Triggers><DataTrigger Binding="{Binding Method}" Value="credit"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                            </Style>
                        </Path.Style>
                    </Path>
                </Grid>
            </Border>
        </ControlTemplate>

        <DataTemplate x:Key="CashTimelineSaleTemplate">
            <Expander Margin="0,0,0,9"
                      IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                      Expanded="TimelineSaleExpander_Expanded">
                <Expander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="{Binding HeaderText}" />
                        <TextBlock Grid.Column="1" Margin="18,0,0,0"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="{Binding PaymentsText}" />
                        <TextBlock Grid.Column="2" Margin="18,0,0,0" FontWeight="Bold"
                                   Foreground="{DynamicResource SuccessBrush}"
                                   Text="{Binding FinalAmountText}" />
                    </Grid>
                </Expander.Header>
                <Border Margin="16,10,16,12" Padding="14"
                        Background="{DynamicResource SurfaceRaisedBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="8">
                    <StackPanel>
                        <TextBlock HorizontalAlignment="Center" Margin="0,12"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="Carregando detalhes da venda...">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasError}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <TextBlock Foreground="{DynamicResource ErrorBrush}" Text="{Binding ErrorMessage}" />
                            <Button Margin="0,8,0,0" HorizontalAlignment="Left" MinWidth="128"
                                    Command="{Binding RetryCommand}" Content="Tentar novamente"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                        </StackPanel>
                        <StackPanel>
                            <StackPanel.Style>
                                <Style TargetType="StackPanel">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasDetail}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </StackPanel.Style>
                            <Grid Margin="0,0,0,12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"
                                               Text="{Binding SaleNumberText}" />
                                    <TextBlock Margin="0,3,0,0" Foreground="{DynamicResource TextSecondaryBrush}"
                                               Text="{Binding DateTimeText}" />
                                    <TextBlock Margin="0,3,0,0" Foreground="{DynamicResource TextSecondaryBrush}"
                                               Text="{Binding SellerText, StringFormat=Operador: {0}}" />
                                </StackPanel>
                                <Border Grid.Column="1" Padding="10,5" CornerRadius="10"
                                        Background="{DynamicResource InfoSurfaceBrush}"
                                        BorderBrush="{DynamicResource InfoBorderBrush}" BorderThickness="{DynamicResource AccessibilityBorderThickness}">
                                    <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold" Foreground="{DynamicResource InfoBrush}"
                                               Text="{Binding StatusText}" />
                                </Border>
                            </Grid>

                            <Border Margin="0,0,0,12" Padding="12"
                                    Background="{DynamicResource ErrorSurfaceBrush}"
                                    BorderBrush="{DynamicResource ErrorBorderBrush}" BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="6">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsCancelled}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock FontWeight="Bold" Foreground="{DynamicResource ErrorBrush}" Text="VENDA CANCELADA" />
                                    <TextBlock Margin="0,5,0,0" Foreground="{DynamicResource TextSecondaryBrush}"
                                               Text="{Binding CancelledByText, StringFormat=Cancelada por: {0}}" />
                                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}"
                                               Text="{Binding CancelledAtText, StringFormat=Data: {0}}" />
                                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" TextWrapping="Wrap"
                                               Text="{Binding CancellationReasonText, StringFormat=Motivo: {0}}" />
                                </StackPanel>
                            </Border>

                            <TextBlock Margin="0,0,0,7" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                       Foreground="{DynamicResource TextSecondaryBrush}" Text="ITENS DA VENDA" />
                            <Border MaxHeight="220" Margin="0,0,0,12" BorderBrush="{DynamicResource BorderBrush}"
                                    BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="6">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Margin="10,7">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Margin="0,0,12,0" Foreground="{DynamicResource TextPrimaryBrush}"
                                                               Text="{Binding Name}" TextTrimming="CharacterEllipsis"
                                                               ToolTip="{Binding Name}" />
                                                    <TextBlock Grid.Column="1" MinWidth="82" HorizontalAlignment="Right"
                                                               Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding UnitPriceText}" />
                                                    <TextBlock Grid.Column="2" MinWidth="58" Margin="10,0" HorizontalAlignment="Center"
                                                               Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding QuantityText}" />
                                                    <TextBlock Grid.Column="3" MinWidth="88" HorizontalAlignment="Right" FontWeight="{DynamicResource FontWeightStrong}"
                                                               Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding SubtotalText}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </ScrollViewer>
                            </Border>

                            <Grid Margin="0,0,0,12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold" Foreground="{DynamicResource TextSecondaryBrush}" Text="RESUMO DA VENDA" />
                                    <TextBlock Margin="0,7,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="Subtotal dos produtos" />
                                    <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="Desconto" />
                                    <TextBlock Margin="0,6,0,0" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Text="Total da venda" />
                                </StackPanel>
                                <StackPanel Grid.Column="1" MinWidth="110">
                                    <TextBlock Text=" " />
                                    <TextBlock Margin="0,7,0,0" HorizontalAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding SubtotalText}" />
                                    <TextBlock Margin="0,4,0,0" HorizontalAlignment="Right" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding DiscountAmountText}" />
                                    <TextBlock Margin="0,6,0,0" HorizontalAlignment="Right" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Text="{Binding FinalAmountText}" />
                                </StackPanel>
                            </Grid>

                            <TextBlock Margin="0,0,0,7" FontSize="{DynamicResource FontSizeSmall}" FontWeight="Bold"
                                       Foreground="{DynamicResource TextSecondaryBrush}" Text="PAGAMENTO" />
                            <ItemsControl Margin="0,0,0,8" ItemsSource="{Binding Payments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Grid Margin="0,0,0,5">
                                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                            <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Text="{Binding Label}" />
                                            <TextBlock Grid.Column="1" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding AmountText}" />
                                        </Grid>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                            <Grid Margin="0,0,0,12">
                                <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Text="Valor recebido" />
                                <TextBlock Grid.Column="1" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding PaidAmountText}" />
                                <TextBlock Grid.Row="1" Margin="0,4,0,0" Foreground="{DynamicResource TextSecondaryBrush}" Text="Troco" />
                                <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,4,0,0" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding ChangeAmountText}" />
                            </Grid>

                            <Grid Margin="0,0,0,12">
                                <Grid.Style>
                                    <Style TargetType="Grid">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers><DataTrigger Binding="{Binding HasProfit}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                                    </Style>
                                </Grid.Style>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                <TextBlock FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextSecondaryBrush}" Text="Lucro da venda" />
                                <TextBlock Grid.Column="1" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Text="{Binding ProfitAmountText}" />
                            </Grid>

                            <Border Padding="12" Background="{DynamicResource InfoSurfaceBrush}"
                                    BorderBrush="{DynamicResource InfoBorderBrush}" BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextSecondaryBrush}" Text="SALDO ANTES DA VENDA" />
                                        <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding BalanceBeforeSaleText}" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="2">
                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextSecondaryBrush}" Text="SALDO APÓS A VENDA" />
                                        <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}" Text="{Binding BalanceAfterSaleText}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </Expander>
        </DataTemplate>
    </UserControl.Resources>
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto"
                  PanningMode="VerticalOnly"
                  ScrollViewer.IsDeferredScrollingEnabled="False">
        <Grid Margin="0,0,8,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="14" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="18" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock FontSize="{DynamicResource FontSizePageTitle}" FontWeight="Bold"
                               Foreground="{DynamicResource TextPrimaryBrush}"
                               Text="Caixa" />
                    <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeBody}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="Abra, acompanhe e feche o caixa da sua adega." />
                </StackPanel>
                <Button Grid.Column="1" MinWidth="108"
                        Command="{Binding RefreshCommand}"
                        Content="Atualizar"
                        Style="{StaticResource SecondaryButtonStyle}" />
            </Grid>

            <StackPanel Grid.Row="2">
                <Border Margin="0,0,0,18" BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="0,0,0,1">
                    <StackPanel Orientation="Horizontal">
                        <Button MinWidth="132" Height="42"
                                Command="{Binding ShowCurrentRegisterTabCommand}"
                                Content="Caixa atual">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0,0,0,3" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsCurrentRegisterTabSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource InfoBrush}" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                        <Button MinWidth="158" Height="42" Margin="8,0,0,0"
                                Command="{Binding ShowPreviousRegistersTabCommand}"
                                Content="Caixas anteriores">
                            <Button.Style>
                                <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                    <Setter Property="Background" Value="Transparent" />
                                    <Setter Property="BorderBrush" Value="Transparent" />
                                    <Setter Property="BorderThickness" Value="0,0,0,3" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsPreviousRegistersTabSelected}" Value="True">
                                            <Setter Property="BorderBrush" Value="{DynamicResource InfoBrush}" />
                                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>
                </Border>
                <Border Padding="12" Margin="0,0,0,8"
                        Background="{DynamicResource ErrorSurfaceBrush}"
                        BorderBrush="{DynamicResource ErrorBorderBrush}"
                        BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="5">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasError}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Foreground="{DynamicResource TextPrimaryBrush}"
                               Text="{Binding ErrorMessage}" TextWrapping="Wrap" />
                </Border>
                <Border Padding="12"
                        Background="{DynamicResource SuccessSurfaceBrush}"
                        BorderBrush="{DynamicResource SuccessBorderBrush}"
                        BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="5">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasSuccess}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Foreground="{DynamicResource TextPrimaryBrush}"
                               Text="{Binding SuccessMessage}" TextWrapping="Wrap" />
                </Border>
            </StackPanel>

            <Border Grid.Row="4" Padding="22"
                    Background="{DynamicResource SurfaceRaisedBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="8">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasNoOpenRegister}" Value="True" />
                                    <Condition Binding="{Binding IsCurrentRegisterTabSelected}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="16" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock FontSize="{DynamicResource FontSizeCardTitle}" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="Caixa fechado" />
                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeBody}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="Informe o valor disponível para iniciar as vendas do turno."
                                   TextWrapping="Wrap" />
                    </StackPanel>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="14" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Margin="0,0,0,7" FontSize="{DynamicResource FontSizeBody}" FontWeight="{DynamicResource FontWeightStrong}"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Text="Valor inicial" />
                            <TextBox Text="{Binding OpeningAmountText, UpdateSourceTrigger=PropertyChanged}"
                                     ToolTip="Valor inicial em reais"
                                     Style="{StaticResource TextInputStyle}" />
                        </StackPanel>
                        <Button Grid.Column="2" MinWidth="156" Margin="0,20,0,0"
                                Command="{Binding OpenCommand}"
                                AutomationProperties.Name="Abrir caixa"
                                Content="Abrir caixa"
                                Style="{StaticResource PrimaryButtonStyle}" />
                    </Grid>
                </Grid>
            </Border>

            <Border Grid.Row="4" Background="Transparent" BorderThickness="0">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding HasOpenRegister}" Value="True" />
                                    <Condition Binding="{Binding IsCurrentRegisterTabSelected}" Value="True" />
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Visibility" Value="Visible" />
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="16" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="16" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock FontSize="{DynamicResource FontSizeTitle}" FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Text="{Binding CurrentRegister.NumberText}" />
                            <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeBody}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Text="Resumo do caixa atual em operação" />
                        </StackPanel>
                        <Border Grid.Column="1" Padding="12,6"
                                Background="{DynamicResource SuccessSurfaceBrush}"
                                BorderBrush="{DynamicResource SuccessBorderBrush}"
                                BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="5">
                            <TextBlock FontWeight="{DynamicResource FontWeightStrong}"
                                       Foreground="{DynamicResource SuccessBrush}"
                                       Text="Aberto" />
                        </Border>
                    </Grid>

                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.05*" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="0.95*" />
                        </Grid.ColumnDefinitions>

                        <Border Padding="20" Background="{DynamicResource SurfaceRaisedBrush}"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="10">
                            <StackPanel>
                                <TextBlock FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="Resumo do caixa" />
                                <TextBlock Margin="0,4,0,16" FontSize="{DynamicResource FontSizeCaption}" Foreground="{DynamicResource TextSecondaryBrush}"
                                           Text="{Binding CurrentRegister.OpenedAtText}" />
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Text="Responsável" />
                                    <TextBlock Grid.Column="1" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}"
                                               Text="{Binding CurrentRegister.ResponsibleUser}" />
                                </Grid>
                                <Grid Margin="0,0,0,10">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}" Text="Vendas realizadas" />
                                    <TextBlock Grid.Column="1" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}"
                                               Text="{Binding CurrentRegister.SalesCountText}" />
                                </Grid>
                                <Border Height="1" Margin="0,4,0,14" Background="{DynamicResource BorderBrush}" />
                                <UniformGrid Columns="3">
                                    <UniformGrid.Style>
                                        <Style TargetType="UniformGrid">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CanViewFinancials}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </UniformGrid.Style>
                                    <StackPanel Margin="0,0,10,0">
                                        <TextBlock FontSize="{DynamicResource FontSizeTiny}" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextMutedBrush}" Text="SALDO INICIAL" />
                                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Text="{Binding CurrentRegister.OpeningAmountText}" />
                                    </StackPanel>
                                    <StackPanel Margin="0,0,10,0">
                                        <TextBlock FontSize="{DynamicResource FontSizeTiny}" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextMutedBrush}" Text="TOTAL VENDIDO" />
                                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource InfoBrush}"
                                                   Text="{Binding CurrentRegister.SalesTotalText}" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock FontSize="{DynamicResource FontSizeTiny}" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextMutedBrush}" Text="SALDO ESPERADO" />
                                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"
                                                   Text="{Binding CurrentRegister.ExpectedAmountText}" />
                                    </StackPanel>
                                </UniformGrid>
                                <Border Margin="0,20,0,0" Padding="14" Background="{DynamicResource SurfaceBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}" BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="7">
                                    <Grid>
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock FontSize="{DynamicResource FontSizeCaption}" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextPrimaryBrush}" Text="Valor contado" />
                                            <TextBox Margin="0,7,0,0" Text="{Binding ClosingAmountText, UpdateSourceTrigger=PropertyChanged}"
                                                     ToolTip="Valor contado no caixa em reais" Style="{StaticResource TextInputStyle}" />
                                        </StackPanel>
                                        <Button Grid.Column="2" MinWidth="138" Margin="0,19,0,0" Command="{Binding CloseCommand}"
                                                AutomationProperties.Name="Fechar caixa"
                                                Content="Fechar caixa" Style="{StaticResource PrimaryButtonStyle}" />
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </Border>

                        <Border Grid.Column="2" Padding="20" Background="{DynamicResource SurfaceRaisedBrush}"
                                BorderBrush="{DynamicResource BorderBrush}" BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="10">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanViewFinancials}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <TextBlock FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="Meios de pagamento" />
                                <TextBlock Margin="0,4,0,14" FontSize="{DynamicResource FontSizeCaption}" Foreground="{DynamicResource TextSecondaryBrush}"
                                           Text="Totais recebidos neste caixa" />
                                <ItemsControl ItemsSource="{Binding CurrentRegister.PaymentTotals}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid Margin="0,0,0,0" MinHeight="44">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="30" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <ContentControl VerticalAlignment="Center"
                                                                AutomationProperties.Name="{Binding Label, StringFormat=Forma de pagamento: {0}}"
                                                                Template="{StaticResource PaymentMethodIconTemplate}" />
                                                <TextBlock Grid.Column="1" VerticalAlignment="Center" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding Label}" />
                                                <TextBlock Grid.Column="2" VerticalAlignment="Center" FontWeight="Bold"
                                                           Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding AmountText}" />
                                                <Border Grid.ColumnSpan="3" Height="1" VerticalAlignment="Bottom" Background="{DynamicResource BorderBrush}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                                <Grid Margin="0,16,0,0">
                                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                                    <TextBlock FontSize="{DynamicResource FontSizeBody}" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource TextSecondaryBrush}" Text="Saldo final esperado" />
                                    <TextBlock Grid.Column="1" FontSize="{DynamicResource FontSizeCardTitle}" FontWeight="Bold" Foreground="{DynamicResource SuccessBrush}"
                                               Text="{Binding CurrentRegister.ExpectedAmountText}" />
                                </Grid>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Border Grid.Row="4" Padding="18"
                            Background="{DynamicResource SurfaceRaisedBrush}"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="10">
                        <StackPanel>
                            <TextBlock FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                       Foreground="{DynamicResource TextPrimaryBrush}"
                                       Text="Histórico de vendas do caixa atual" />
                            <TextBlock Margin="0,4,0,12" FontSize="{DynamicResource FontSizeCaption}"
                                       Foreground="{DynamicResource TextSecondaryBrush}"
                                       Text="Clique em uma venda para conferir o saldo acumulado antes e depois dela." />
                            <TextBlock Foreground="{DynamicResource TextSecondaryBrush}"
                                       Text="Nenhuma venda registrada neste caixa.">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding HasNoCurrentTimeline}" Value="True">
                                                <Setter Property="Visibility" Value="Visible" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <ItemsControl ItemsSource="{Binding CurrentTimeline}"
                                          ItemTemplate="{StaticResource CashTimelineSaleTemplate}" />
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Row="4">
                <StackPanel.Style>
                    <Style TargetType="StackPanel">
                        <Setter Property="Visibility" Value="Collapsed" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsPreviousRegistersTabSelected}" Value="True">
                                <Setter Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </StackPanel.Style>
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock FontSize="{DynamicResource FontSizeCardTitle}" FontWeight="Bold" Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="Histórico de caixas" />
                        <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeCaption}" Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="Selecione uma linha para consultar resumo, pagamentos e movimentações." />
                    </StackPanel>
                    <Button Grid.Column="1" MinWidth="112" Command="{Binding RefreshCommand}"
                            Content="Atualizar lista" Style="{StaticResource SecondaryButtonStyle}" />
                </Grid>
                <DataGrid ItemsSource="{Binding RecentRegisters}"
                          x:Name="RecentRegistersGrid"
                          MaxHeight="620"
                          SelectedItem="{Binding SelectedRegister, Mode=TwoWay}"
                          PreviewMouseLeftButtonDown="RecentRegistersGrid_PreviewMouseLeftButtonDown"
                          SelectionChanged="RecentRegistersGrid_SelectionChanged"
                          AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" CanUserAddRows="False"
                          CanUserDeleteRows="False" CanUserReorderColumns="False"
                          CanUserResizeRows="False" SelectionMode="Single"
                          RowDetailsVisibilityMode="VisibleWhenSelected"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling"
                          VirtualizingPanel.ScrollUnit="Pixel"
                          ScrollViewer.CanContentScroll="True"
                          ScrollViewer.IsDeferredScrollingEnabled="False"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          Background="{DynamicResource SurfaceRaisedBrush}"
                          Foreground="{DynamicResource TextPrimaryBrush}"
                          BorderBrush="{DynamicResource BorderBrush}"
                          HorizontalGridLinesBrush="{DynamicResource BorderBrush}"
                          VerticalGridLinesBrush="Transparent"
                          RowBackground="{DynamicResource SurfaceRaisedBrush}"
                                      AlternatingRowBackground="{DynamicResource AlternatingRowBrush}"
                          RowHeight="54" ColumnHeaderHeight="40">
                    <DataGrid.Style>
                        <Style TargetType="DataGrid">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasRecentRegisters}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.Style>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Setter Property="Cursor" Value="Hand" />
                            <Setter Property="ToolTip" Value="Clique para abrir; clique novamente para recolher" />
                            <Style.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource InfoSurfaceBrush}" />
                                    <Setter Property="BorderBrush" Value="{DynamicResource InfoBorderBrush}" />
                                    <Setter Property="BorderThickness" Value="0,1,0,1" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Ação" Width="78">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Padding="8,4" HorizontalAlignment="Center" VerticalAlignment="Center"
                                            Background="{DynamicResource InfoSurfaceBrush}" CornerRadius="5">
                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}" Foreground="{DynamicResource InfoBrush}">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Abrir" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=DataGridRow}}" Value="True">
                                                            <Setter Property="Text" Value="Recolher" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Caixa" Binding="{Binding NumberText}" Width="0.8*" />
                        <DataGridTextColumn Header="Abertura" Binding="{Binding OpenedAtText}" Width="1.25*" />
                        <DataGridTextColumn Header="Fechamento" Binding="{Binding ClosedAtText}" Width="1.25*" />
                        <DataGridTextColumn Header="Responsável" Binding="{Binding ResponsibleUser}" Width="1.1*" />
                        <DataGridTextColumn Header="Vendas" Binding="{Binding SalesCountText}" Width="0.85*" />
                        <DataGridTextColumn Header="Total" Binding="{Binding SalesTotalText}" Width="0.9*" />
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="0.75*" />
                    </DataGrid.Columns>
                    <DataGrid.RowDetailsTemplate>
                        <DataTemplate>
                            <Border Margin="10,0,10,14" Padding="18"
                                    DataContext="{Binding DataContext, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    Background="{DynamicResource SurfaceBrush}"
                                    BorderBrush="{DynamicResource InfoBorderBrush}"
                                    BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="10">
                                <StackPanel>
                                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}"
                                               Text="Carregando detalhes do caixa...">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsDetailLoading}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <StackPanel>
                                        <StackPanel.Style>
                                            <Style TargetType="StackPanel">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding HasDetail}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </StackPanel.Style>
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel>
                                                <TextBlock FontSize="{DynamicResource FontSizeSection}" FontWeight="Bold"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           Text="{Binding DetailRegister.NumberText}" />
                                                <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeCaption}"
                                                           Foreground="{DynamicResource TextSecondaryBrush}">
                                                    <Run Text="{Binding DetailRegister.ResponsibleUser, Mode=OneWay}" />
                                                    <Run Text=" • " />
                                                    <Run Text="{Binding DetailRegister.StatusText, Mode=OneWay}" />
                                                </TextBlock>
                                            </StackPanel>
                                            <Button Grid.Column="1" MinWidth="128"
                                                    Command="{Binding LoadRegisterDetailCommand}"
                                                    Content="Atualizar detalhe"
                                                    Style="{StaticResource SecondaryButtonStyle}" />
                                        </Grid>

                                        <UniformGrid Margin="0,16,0,0" Columns="4">
                                            <StackPanel Margin="0,0,14,12">
                                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextMutedBrush}" Text="ABERTURA" />
                                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           Text="{Binding DetailRegister.OpenedAtText}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Margin="0,0,14,12">
                                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextMutedBrush}" Text="FECHAMENTO" />
                                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           Text="{Binding DetailRegister.ClosedAtText}" TextWrapping="Wrap" />
                                            </StackPanel>
                                            <StackPanel Margin="0,0,14,12">
                                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextMutedBrush}" Text="VENDAS" />
                                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                                           Text="{Binding DetailRegister.SalesCountText}" />
                                            </StackPanel>
                                            <StackPanel Margin="0,0,0,12">
                                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextMutedBrush}" Text="TOTAL VENDIDO" />
                                                <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                                           Foreground="{DynamicResource InfoBrush}"
                                                           Text="{Binding DetailRegister.SalesTotalText}" />
                                            </StackPanel>
                                        </UniformGrid>

                                        <Border Margin="0,0,0,14" Padding="12"
                                    Background="{DynamicResource WarningSoftBrush}" BorderBrush="{DynamicResource WarningBrush}"
                                                BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="7">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding DetailRegister.HasCancellationAdjustment}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                            <TextBlock FontWeight="Bold" Foreground="{DynamicResource WarningBrush}"
                                                           Text="Ajuste posterior ao fechamento" />
                                                <TextBlock Margin="0,4,0,0" Foreground="{DynamicResource TextSecondaryBrush}"
                                                           Text="{Binding DetailRegister.CancellationAdjustmentText}" TextWrapping="Wrap" />
                                                <TextBlock Margin="0,3,0,0" FontSize="{DynamicResource FontSizeCaption}" Foreground="{DynamicResource TextMutedBrush}"
                                                           Text="A conferência original foi preservada." />
                                            </StackPanel>
                                        </Border>

                                        <Border Margin="0,4,0,0" Padding="14"
                                                Background="{DynamicResource InfoSurfaceBrush}"
                                                BorderBrush="{DynamicResource InfoBorderBrush}"
                                                BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="7">
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CanViewFinancials}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                            <StackPanel>
                                                <UniformGrid Columns="3">
                                                    <StackPanel Margin="0,0,12,10">
                                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextMutedBrush}"
                                                                   Text="VALOR INICIAL" />
                                                        <TextBlock Margin="0,4,0,0" FontWeight="Bold"
                                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                                   Text="{Binding DetailRegister.OpeningAmountText}" />
                                                    </StackPanel>
                                                    <StackPanel Margin="0,0,12,10">
                                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextMutedBrush}"
                                                                   Text="VALOR FINAL" />
                                                        <TextBlock Margin="0,4,0,0" FontWeight="Bold"
                                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                                   Text="{Binding DetailRegister.ClosingAmountText}" />
                                                    </StackPanel>
                                                    <StackPanel Margin="0,0,0,10">
                                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" Foreground="{DynamicResource TextMutedBrush}"
                                                                   Text="VALOR ESPERADO" />
                                                        <TextBlock Margin="0,4,0,0" FontWeight="Bold"
                                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                                   Text="{Binding DetailRegister.ExpectedAmountText}" />
                                                    </StackPanel>
                                                </UniformGrid>
                                                <TextBlock Margin="0,4,0,8" FontSize="{DynamicResource FontSizeCaption}" FontWeight="{DynamicResource FontWeightStrong}"
                                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                                           Text="Formas de pagamento" />
                                                <ItemsControl ItemsSource="{Binding DetailRegister.PaymentTotals}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <Grid MinHeight="38">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="24" />
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="Auto" />
                                                                </Grid.ColumnDefinitions>
                                                                <ContentControl VerticalAlignment="Center"
                                                                                AutomationProperties.Name="{Binding Label, StringFormat=Forma de pagamento: {0}}"
                                                                                Template="{StaticResource PaymentMethodIconTemplate}">
                                                                    <ContentControl.LayoutTransform><ScaleTransform ScaleX="0.8" ScaleY="0.8" /></ContentControl.LayoutTransform>
                                                                </ContentControl>
                                                                <TextBlock Grid.Column="1" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"
                                                                           Text="{Binding Label}" />
                                                                <TextBlock Grid.Column="2" VerticalAlignment="Center" FontWeight="Bold"
                                                                           Foreground="{DynamicResource TextPrimaryBrush}" Text="{Binding AmountText}" />
                                                                <Border Grid.ColumnSpan="3" Height="1" VerticalAlignment="Bottom" Background="{DynamicResource BorderBrush}" />
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>

                                        <TextBlock Margin="0,18,0,10" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Text="Linha do tempo de vendas" />
                                        <TextBlock Margin="0,0,0,8" FontSize="{DynamicResource FontSizeCaption}"
                                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                                   Text="Nenhuma venda vinculada a este caixa.">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding HasNoTimeline}" Value="True">
                                                            <Setter Property="Visibility" Value="Visible" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                        <ListBox MaxHeight="360"
                                                 ItemsSource="{Binding Timeline}"
                                                 Background="Transparent"
                                                 BorderThickness="0"
                                                 ScrollViewer.CanContentScroll="True"
                                                 ScrollViewer.IsDeferredScrollingEnabled="False"
                                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                                                 VirtualizingPanel.IsVirtualizing="True"
                                                 VirtualizingPanel.VirtualizationMode="Recycling"
                                                 VirtualizingPanel.ScrollUnit="Pixel">
                                            <ListBox.ItemsPanel>
                                                <ItemsPanelTemplate><VirtualizingStackPanel /></ItemsPanelTemplate>
                                            </ListBox.ItemsPanel>
                                            <ListBox.ItemContainerStyle>
                                                <Style TargetType="ListBoxItem">
                                                    <Setter Property="Padding" Value="0" />
                                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                                    <Setter Property="Background" Value="Transparent" />
                                                    <Setter Property="Focusable" Value="False" />
                                                    <Setter Property="Template">
                                                        <Setter.Value>
                                                            <ControlTemplate TargetType="ListBoxItem">
                                                                <ContentPresenter />
                                                            </ControlTemplate>
                                                        </Setter.Value>
                                                    </Setter>
                                                </Style>
                                            </ListBox.ItemContainerStyle>
                                            <ListBox.ItemTemplate>
                                                <DataTemplate>
                                                    <ContentControl Content="{Binding}"
                                                                    ContentTemplate="{StaticResource CashTimelineSaleTemplate}" />
                                                </DataTemplate>
                                            </ListBox.ItemTemplate>
                                        </ListBox>
                                    </StackPanel>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </DataGrid.RowDetailsTemplate>
                </DataGrid>

                <Border Margin="0,14,0,0" Padding="22"
                        Background="{DynamicResource SurfaceRaisedBrush}"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasNoRecentRegisters}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="{DynamicResource FontWeightStrong}"
                                   HorizontalAlignment="Center"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="Nenhum caixa anterior encontrado" />
                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="Os caixas encerrados aparecerão aqui." />
                    </StackPanel>
                </Border>

                <Grid Margin="0,12,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock VerticalAlignment="Center" FontSize="{DynamicResource FontSizeCaption}"
                               Foreground="{DynamicResource TextSecondaryBrush}"
                               Text="Clique em qualquer caixa para abrir vendas, itens e pagamentos." />
                    <Button Grid.Column="1" MinWidth="150"
                            Command="{Binding LoadRegisterDetailCommand}"
                            Content="Atualizar detalhe"
                            Style="{StaticResource SecondaryButtonStyle}" />
                </Grid>

                <Border Margin="0,14,0,0" Padding="12" Visibility="Collapsed"
                        Background="{DynamicResource InfoSurfaceBrush}"
                        BorderBrush="{DynamicResource InfoBorderBrush}"
                        BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="6">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDetailLoading}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Foreground="{DynamicResource TextPrimaryBrush}"
                               Text="Carregando detalhes do caixa..." />
                </Border>

                <Border Margin="0,16,0,0" Padding="18" Visibility="Collapsed"
                        Background="{DynamicResource SurfaceRaisedBrush}"
                        BorderBrush="{DynamicResource InfoBorderBrush}"
                        BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="8">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasDetail}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock FontSize="{DynamicResource FontSizeSection}" FontWeight="Bold"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="{Binding DetailRegister.NumberText}" />
                                <TextBlock Margin="0,4,0,0" FontSize="{DynamicResource FontSizeCaption}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Text="{Binding DetailRegister.StatusText}" />
                            </StackPanel>
                            <Button Grid.Column="1" MinWidth="118"
                                    Command="{Binding ClearRegisterDetailCommand}"
                                    Content="Fechar"
                                    Style="{StaticResource SecondaryButtonStyle}" />
                        </Grid>

                        <UniformGrid Margin="0,16,0,0" Columns="4">
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           Text="ABERTURA" />
                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="{Binding DetailRegister.OpenedAtText}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           Text="FECHAMENTO" />
                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="{Binding DetailRegister.ClosedAtText}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel Margin="0,0,12,0">
                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           Text="RESPONSÁVEL" />
                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="{Binding DetailRegister.ResponsibleUser}"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <StackPanel>
                                <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextMutedBrush}"
                                           Text="VENDAS" />
                                <TextBlock Margin="0,5,0,0" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextPrimaryBrush}"
                                           Text="{Binding DetailRegister.SalesCountText}" />
                            </StackPanel>
                        </UniformGrid>

                        <Border Margin="0,16,0,0" Padding="14"
                                Background="{DynamicResource InfoSurfaceBrush}"
                                BorderBrush="{DynamicResource InfoBorderBrush}"
                                BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="6">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding CanViewFinancials}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <StackPanel>
                                <UniformGrid Columns="3">
                                    <StackPanel Margin="0,0,12,0">
                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   Text="VALOR INICIAL" />
                                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Text="{Binding DetailRegister.OpeningAmountText}" />
                                    </StackPanel>
                                    <StackPanel Margin="0,0,12,0">
                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   Text="TOTAL VENDIDO" />
                                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                                   Foreground="{DynamicResource InfoBrush}"
                                                   Text="{Binding DetailRegister.SalesTotalText}" />
                                    </StackPanel>
                                    <StackPanel>
                                        <TextBlock FontSize="{DynamicResource FontSizeSmall}" FontWeight="{DynamicResource FontWeightStrong}"
                                                   Foreground="{DynamicResource TextMutedBrush}"
                                                   Text="FECHAMENTO" />
                                        <TextBlock Margin="0,5,0,0" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                                   Text="{Binding DetailRegister.ClosingAmountText}" />
                                    </StackPanel>
                                </UniformGrid>

                                <TextBlock Margin="0,14,0,8" FontSize="{DynamicResource FontSizeCaption}" FontWeight="{DynamicResource FontWeightStrong}"
                                           Foreground="{DynamicResource TextSecondaryBrush}"
                                           Text="Pagamentos do caixa" />
                                <ItemsControl ItemsSource="{Binding DetailRegister.PaymentTotals}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border Margin="0,0,10,8" Padding="12,8"
                                                    Background="{DynamicResource SurfaceRaisedBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="{DynamicResource AccessibilityBorderThickness}" CornerRadius="5">
                                                <StackPanel Orientation="Horizontal">
                                                    <ContentControl Margin="0,0,8,0" VerticalAlignment="Center"
                                                                    AutomationProperties.Name="{Binding Label, StringFormat=Forma de pagamento: {0}}"
                                                                    Template="{StaticResource PaymentMethodIconTemplate}">
                                                        <ContentControl.LayoutTransform><ScaleTransform ScaleX="0.8" ScaleY="0.8" /></ContentControl.LayoutTransform>
                                                    </ContentControl>
                                                    <TextBlock Foreground="{DynamicResource TextSecondaryBrush}"
                                                               Text="{Binding Label}" />
                                                    <TextBlock Margin="8,0,0,0" FontWeight="Bold"
                                                               Foreground="{DynamicResource TextPrimaryBrush}"
                                                               Text="{Binding AmountText}" />
                                                </StackPanel>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </Border>

                        <TextBlock Margin="0,18,0,10" FontSize="{DynamicResource FontSizeSubtitle}" FontWeight="Bold"
                                   Foreground="{DynamicResource TextPrimaryBrush}"
                                   Text="Linha do tempo de vendas" />
                        <TextBlock Margin="0,0,0,8" FontSize="{DynamicResource FontSizeCaption}"
                                   Foreground="{DynamicResource TextSecondaryBrush}"
                                   Text="Nenhuma venda vinculada a este caixa.">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasNoTimeline}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ItemsControl ItemsSource="{Binding Timeline}"
                                      ItemTemplate="{StaticResource CashTimelineSaleTemplate}" />
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
